# Curated UI captures

These screenshots document the review UI layout. They were captured during
development with `USE_MOCK_CANDIDATES=true`, so a mock-mode banner and
`.example.com` fixture URLs may appear.

They contain **no** real passwords, API keys, service-account JSON, or private
spreadsheet IDs. Prefer regenerating captures with mock mode off before a
marketing launch if you want production-like chrome.
