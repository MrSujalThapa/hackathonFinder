import { ControlledApplicationForm } from "@/components/fixtures/ControlledApplicationForm";

/** Safe local target used exclusively for Pass 2 browser automation acceptance tests. */
export default function ControlledApplicationFixturePage() { return <ControlledApplicationForm />; }
