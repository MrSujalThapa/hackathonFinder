import { AssetBankEditor } from "@/components/applications/AssetBankEditor";
export default function AssetsPage() { return <section className="mx-auto max-w-2xl space-y-5"><header><p className="font-mono text-xs text-muted">REUSABLE FILES & LINKS</p><h1 className="hf-doc-title text-3xl">Asset Bank</h1><p className="mt-2 text-muted">Save reusable file references and URLs. Draft-specific uploads never overwrite these automatically.</p></header><AssetBankEditor /></section>; }
