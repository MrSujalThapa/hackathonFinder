import { ApplicationEditor } from "@/components/applications/ApplicationEditor";
export default async function DraftPage({ params }: { params: Promise<{ id: string }> }) { return <section className="mx-auto max-w-3xl space-y-5"><header><p className="font-mono text-xs text-muted">APPLICATION DRAFT</p><h1 className="hf-doc-title text-3xl">Continue application</h1><p className="mt-2 text-muted">Every answer is editable. Real-form preview stops before submission; Submit now is explicit and version-bound.</p></header><ApplicationEditor id={(await params).id} /></section>; }
