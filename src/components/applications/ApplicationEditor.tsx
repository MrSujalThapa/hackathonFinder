"use client";
import { useCallback, useEffect, useState } from "react";
import type { ApplicationDraft } from "@/core/applications/types";
export function ApplicationEditor({ id }: { id: string }) {
  const [application, setApplication] = useState<ApplicationDraft | null>(null); const [message, setMessage] = useState(""); const [state, setState] = useState<"loading" | "loaded" | "not_found" | "unauthorized" | "error">("loading"); const [submitChoices, setSubmitChoices] = useState(false);
  const load = useCallback(async () => { setState("loading"); try { const response = await fetch(`/api/applications/${id}`); const body = await response.json().catch(() => null) as { data?: { application?: ApplicationDraft }; error?: { message?: string } } | null; if (response.status === 404) { setState("not_found"); return; } if (response.status === 401 || response.status === 403) { setMessage(body?.error?.message ?? "You are not allowed to view this draft."); setState("unauthorized"); return; } if (!response.ok || !body?.data?.application) { setMessage(body?.error?.message ?? "Could not load this draft."); setState("error"); return; } setApplication(body.data.application); setState("loaded"); } catch { setMessage("Could not load this draft. Check your connection and retry."); setState("error"); } }, [id]);
  useEffect(() => { void load(); }, [load]);
  async function action(body: Record<string, string>) { const response = await fetch(`/api/applications/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const payload = await response.json() as { data?: { application?: ApplicationDraft; message?: string }; error?: { message?: string } }; setMessage(payload.data?.message ?? payload.error?.message ?? (response.ok ? "Saved." : "Could not save.")); if (payload.data?.application) setApplication(payload.data.application); }
  if (state === "loading") return <p aria-busy="true" className="text-muted">Loading application…</p>;
  if (state === "not_found") return <div role="alert" className="hf-sheet-frame p-4"><h2 className="font-medium">Draft not found</h2><p className="mt-1 text-sm text-muted">It may have been deleted or the link is incorrect.</p></div>;
  if (state === "unauthorized") return <div role="alert" className="hf-sheet-frame p-4"><h2 className="font-medium">Draft unavailable</h2><p className="mt-1 text-sm text-muted">{message}</p></div>;
  if (state === "error" || !application) return <div role="alert" className="hf-sheet-frame space-y-3 p-4"><h2 className="font-medium">Could not load draft</h2><p className="text-sm text-muted">{message}</p><button className="hf-focus border border-border px-3 py-2 text-sm" onClick={() => void load()}>Retry</button></div>;
  const blockers = application.questions.filter((question) => question.required && !question.answer);
  return <div className="mx-auto max-w-3xl space-y-5"><div className="border-b-2 border-border pb-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="hf-technical-label">{application.status.replaceAll("_", " ")}</p><h2 className="hf-doc-title mt-1 text-2xl">Application draft</h2><p className="mt-1 text-xs text-muted">{application.currentPage ? `Page ${application.currentPage}${application.totalPages ? `/${application.totalPages}` : ""}` : "Form checkpoint saved"} · Draft v{application.draftVersion}</p></div><a className="hf-btn hf-btn-ghost" href={application.applicationUrl} target="_blank" rel="noreferrer">Open form</a></div></div>{blockers.length ? <div className="border-l-2 border-accent-warn bg-inset px-4 py-3 text-sm"><strong>{blockers.length} blocker{blockers.length === 1 ? "" : "s"}</strong><p className="mt-1 text-muted">Complete the required fields, then choose Save & Continue. It will never submit the external application.</p></div> : null}<div className="bg-[color-mix(in_oklab,var(--surface)_92%,white)] px-4 sm:px-8">{application.questions.map((question) => <section className="border-b border-border py-7" key={question.id}><div className="flex items-start justify-between gap-3"><label className="font-medium" htmlFor={question.id}>{question.label}{question.required ? " *" : " (optional)"}</label><span className="hf-status-stamp">{question.answerSource.replaceAll("_", " ")}</span></div><textarea id={question.id} className="hf-input mt-4 min-h-32 resize-y leading-6" defaultValue={question.answer ?? ""} onBlur={(event) => { if (event.target.value !== (question.answer ?? "")) void action({ action: "edit", questionId: question.id, answer: event.target.value }); }} /><p className="mt-2 text-xs text-muted">{question.needsUserInput ? "Required blocker — your answer is needed" : "Editable; this answer can always be changed."}</p></section>)}</div>{submitChoices ? <div role="dialog" aria-label="Submit application" className="hf-sheet-frame space-y-3 p-4"><h2 className="font-medium">How do you want to proceed?</h2><p className="text-sm text-muted">Viewing fills the actual form and stops before submission. Submit now authorizes only draft v{application.draftVersion}.</p><div className="flex flex-wrap gap-2"><button className="hf-btn hf-btn-ghost" onClick={() => void action({ action: "preview" })}>View real form</button><button className="hf-btn hf-btn-primary" onClick={() => { setSubmitChoices(false); void action({ action: "submit_now" }); }}>Submit now</button><button className="hf-btn hf-btn-ghost" onClick={() => setSubmitChoices(false)}>Cancel</button></div></div> : null}<div className="sticky bottom-0 flex flex-wrap gap-2 border border-border bg-surface/95 p-3 backdrop-blur"><button className="hf-btn hf-btn-primary" onClick={() => void action({ action: "save" })}>Save</button><button className="hf-btn hf-btn-primary" onClick={() => void action({ action: "save_continue" })} disabled={application.status === "user_managed" || application.status === "submitted"}>Save &amp; Continue</button>{application.status === "ready_to_submit" ? <button className="hf-btn hf-btn-primary" onClick={() => setSubmitChoices(true)}>Submit</button> : null}<button className="hf-btn hf-btn-ghost" onClick={() => void action({ action: "pause" })}>Pause</button><button className="hf-btn hf-btn-ghost" onClick={() => void action({ action: "do_myself" })}>Do this myself</button><button className="hf-btn hf-btn-ghost text-accent-danger" onClick={() => { if (window.confirm("Delete this draft? This cannot be undone.")) void action({ action: "delete" }); }}>Delete draft</button></div><p role="status" className="text-sm text-muted">{message}</p></div>;
}
